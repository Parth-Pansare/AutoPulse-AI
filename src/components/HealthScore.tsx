import type { ReactNode } from 'react';
import { Activity, Battery, Fuel } from 'lucide-react';
export default function HealthScore(){return <section className="health-score-card card"><div className="section-kicker">OVERALL VEHICLE HEALTH <span className="status-pill good">● HEALTHY</span></div><div className="health-main"><div className="score-ring"><div><strong>87</strong><small>/100</small><span>GOOD</span></div></div><div className="health-copy"><h2>Vehicle is in good condition</h2><p>No critical issues detected. Keep following the recommended maintenance schedule.</p><div className="health-bars"><Bar icon={<Activity/>} label="Engine" value={92} type="cyan"/><Bar icon={<Battery/>} label="Battery" value={85} type="blue"/><Bar icon={<Fuel/>} label="Fuel System" value={78} type="orange"/></div></div></div></section>}
function Bar({icon,label,value,type}:{icon:ReactNode;label:string;value:number;type:string}){return <div className="health-bar"><div className={`metric-icon ${type}`}>{icon}</div><span>{label}</span><b>{value}%</b><div className="bar-track"><i className={type} style={{width:`${value}%`}}/></div></div>}
